# Copy to .env and fill real values. NEVER commit .env.
# Each tool reads only keys it needs.

# Example API keys
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# Google OAuth (paths to gitignored files)
GOOGLE_CREDENTIALS_PATH=credentials.json
GOOGLE_TOKEN_PATH=token.json
